# TornFi Community — 환경 변수 예시 (실제 값은 .env에 넣고, .env는 Git에 올리지 마세요)

# 프로덕션에서 필수 (16자 이상 랜덤 문자열 권장)
SESSION_SECRET=your-32-char-or-longer-random-secret

# 프로덕션일 때 설정 (development | production)
NODE_ENV=production

# 프로덕션에서 www / 비-www 또는 서브도메인(api.도메인) 쿠키 공유: .도메인.com 형태로 설정
# COOKIE_DOMAIN=.tornfi.com
# 프론트(도메인.com)와 API(api.도메인.com)가 다를 때만. SameSite=None 사용(HTTPS 필수)
# COOKIE_SAMESITE=none

# 서버 포트 (선택, 기본 3000)
# PORT=3000

# MongoDB 연결 문자열 — 영구 보관용 (권장)
# 설정 시: 회원·게시글·채팅·설정 등이 MongoDB에 저장되며, 배포/재시작해도 유지됩니다.
# 미설정 시: data/*.json 파일 사용 (프로젝트 폴더 덮어쓰기 시 데이터 손실 가능)
# ⚠ URI에 DB 이름 필수. 없으면 기본 DB "test"만 사용 → Atlas에서 TornFi 등 다른 DB에 넣은 데이터는 안 보임. 예: ...mongodb.net/tornfi?...
# 사용법: MongoDB Atlas(https://www.mongodb.com/cloud/atlas) 무료 클러스터 생성 → 연결 주소 복사 → 아래 주석 해제 후 붙여넣기
# MONGODB_URI=mongodb+srv://user:password@cluster0.xxxxx.mongodb.net/tornfi?retryWrites=true&w=majority
#
# ⚠ 배포(서버) 시: localhost/127.0.0.1 사용 금지. Atlas 또는 DB 서버의 공인 주소·계정을 사용하세요.

# 관리자 지갑 주소 (쉼표 구분, 소문자). 이 주소로 가입한 회원은 관리자 메뉴 표시
# ADMIN_WALLET_ADDRESSES=0xabc...,0xdef...

# 이더리움 RPC (스테이킹 조회 등). Infura/Alchemy URL
# ETH_RPC_URL=https://mainnet.infura.io/v3/YOUR_KEY

# DeepL API 키 (뉴스 번역, 없으면 MyMemory 사용)
# DEEPL_AUTH_KEY=your-deepl-api-key

# Etherscan API 키 (등급 동기화 등, 선택)
# ETHERSCAN_API_KEY=your-etherscan-api-key

# 토네이도 뉴스 자동 수집 주기(ms). 기본 6시간
# TORNADO_NEWS_FETCH_INTERVAL_MS=21600000

# CORS: 프론트가 백엔드와 다른 URL일 때 (예: 프론트 Vercel, 백 Render). 설정 시 해당 오리진에서 쿠키/세션 전송 허용
# CLIENT_URL=https://your-frontend.vercel.app
